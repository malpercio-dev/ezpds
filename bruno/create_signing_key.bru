meta {
  name: Create Signing Key
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/v1/pds/keys
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "algorithm": "p256"
  }
}
