meta {
  name: Delete Record
  type: http
  seq: 34
}

post {
  url: {{baseUrl}}/xrpc/com.atproto.repo.deleteRecord?did={{did}}&collection={{collection}}&rkey={{rkey}}
  body: none
  auth: bearer
}

params:query {
  did: {{did}}
  collection: {{collection}}
  rkey: {{rkey}}
}

auth:bearer {
  token: {{accessToken}}
}
