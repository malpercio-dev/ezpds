Scanning a sign-in QR with the iPhone Camera app (instead of from inside Obsign) now opens the consent approval screen directly — including working out which of several identities the request is for — where it previously just landed on the overview.
