Internal: the wallet's Bearer session refresh (`com.atproto.server.refreshSession`) network call and sub/aud re-validation moved into the `custos-client` crate; the wallet keeps the refresh decision ladder, coalescing lock, and Keychain persistence. No user-visible or behavioral change.
