<task-notification>
<task-id>af2f6ab1af019567b</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jacob-zweifel-workspace-malpercio-dev-ezpds--claude-worktrees-awesome-lamport-bf2e56/d99ceb90-e7e1-42a0-bc40-c5ae3369966b/tasks/af2f6ab1af019567b.output</output-file>
<status>completed</status>
<summary>Agent "Rewire migration UI to main's API" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Confirmed clean: 342 files, 0 errors, 0 warnings.

## Summary

Built the MM-232 outbound-migration UI in the worktree at `/Users/jacob.zweifel/workspace/malpercio-dev/ezpds/.claude/worktrees/awesome-lamport-bf2e56/apps/identity-wallet`. Did not touch `ipc.ts` or any Rust file (verified `ipc.ts`'s diff pre-existed my session — it was the `authenticateBiometric` addition already staged in the worktree).

**Files created** (`src/lib/components/onboarding/`): `MigrationStartScreen.svelte`, `MigrationSourceAuthScreen.svelte`, `MigrationProgressScreen.svelte`, `MigrationReviewScreen.svelte`, `MigrationSuccessScreen.svelte` — adapted from the SHA `026fb51f` versions, rewired against the real orchestrator API.

**Files modified**: `src/routes/+page.svelte` (added the 5 migration steps + state threading), `src/lib/components/home/DIDDocumentScreen.svelte` (added optional `onmigrate` prop + button — the entry point didn't exist yet on this branch), `src/lib/components/home/IdentityListHome.svelte` (extended `onselect` to pass `deviceKeyIsRoot`, needed to gate the entry point to wallet-controlled identities).

**Screen → API mapping:**
- **MigrationStartScreen** (`did, onnext, onback`): calls `detectMigrationPath(did)`, branches on `path` (`self_signed` → `prepareMigration(did, destPdsUrl)` → `onnext`; `interop` → terminal message, no retry; `cannot_determine` → retry message). Dropped `currentHandle`/handle field, added invite-code field.
- **MigrationSourceAuthScreen** (`did, onnext, onback`): calls `startSourceAuth(did)`, retries on failure.
- **MigrationProgressScreen** (`did, email, inviteCode, onnext, onerror`): runs `createDestinationAccount → transferRepo → transferBlobs → transferPreferences → verifyImport` sequentially; removed the `MIGRATION_BLOB_PROGRESS_EVENT` listener and live blob counter entirely (no such event on main); leg checklist keeps glyph+word+position status.
- **MigrationReviewScreen** (`did, onnext, oncancel`): on mount runs `armIdentityLeg(did)` then `buildMigrationOp(did)`; "Approve with Face ID" gates on `authenticateBiometric` before `submitMigrationOp` → `finalizeMigration`.
- **MigrationSuccessScreen** (`result, destPdsLabel?, ondone`): unchanged logic, `destPdsLabel` made optional per the new signature.

**Two error types:** `MigrationError` (orchestrator: `detectMigrationPath`/`prepareMigration`/`startSourceAuth`/transfer legs/`armIdentityLeg`/`finalizeMigration`) and `MigrateError` (identity leg: `buildMigrationOp`/`submitMigrationOp`) both narrow via `isCodedError`. `MigrationReviewScreen.describeError` accepts `MigrationError | MigrateError` as a union and switches on the combined code space — since Rust enums for both are disjoint but structurally identical (`{code, message?}` or `{code, reason}` for `GUARD_REJECTED`), one function classifies both.

**CodeRabbit fixes:**
- **Progress screen**: added a `failure` state that renders the failed leg's message inline with a `Retry` button (restarts only the failed+later legs), instead of relying solely on `onerror` to rewind silently. `+page.svelte`'s `onerror` handler now just stores the error and stays on `migration_progress` rather than rewinding to `migration_source_auth`.
- **Review screen**: added `loadErrorIsTerminal` (persisted alongside `loadError`, not just the message) computed from a `TERMINAL_CODES` set (`WALLET_NOT_AUTHORIZED`, `GUARD_REJECTED`, `IDENTITY_NOT_FOUND`, `MIGRATION_NOT_READY`, `DESTINATION_CONFLICT`, plus `INVALID_RECOMMENDED_CREDENTIALS`/`INVALID_AUDIT_LOG`); the Retry button is hidden when the on-mount load error is terminal, mirroring the existing submit-path terminal handling (`errorIsTerminal`/disabled Approve button).

**Verify:** `pnpm check` — `342 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`, exit code 0. No sandbox EPERM encountered.

**Uncertainties / judgment calls:**
- The prompt's `MigrationProgressScreen` spec doesn't include `did-doc-utils` handle/PDS extraction since `MigrationSourceAuthScreen` dropped `sourcePdsLabel` — matches the prompt's stated prop list exactly (`{ did, onnext, onback }`), so I removed the PDS chip UI that referenced it.
- The entry point ("Migrate to another PDS" on identity-detail, gated to wallet-controlled identities) did not yet exist in this worktree's `+page.svelte`/`DIDDocumentScreen`/`IdentityListHome` — I added it (mirroring the historical SHA), extending `IdentityListHome.onselect`'s signature and adding `DIDDocumentScreen.onmigrate`. These are not `ipc.ts`/Rust files, so in scope per the instructions, but flagging since it's more than a straight "rewire" of the 5 screens.
- `migrationDecision` and `migrationVerifyStatus` are stored on `+page.svelte` for parity with the old flow (in case a later screen needs them) but currently unread past assignment — harmless under `pnpm check` (Svelte doesn't flag unused `$state` fields), kept for symmetry with the original design rather than trimmed.</result>
<usage><subagent_tokens>202955</subagent_tokens><tool_uses>53</tool_uses><duration_ms>344401</duration_ms></usage>
</task-notification>