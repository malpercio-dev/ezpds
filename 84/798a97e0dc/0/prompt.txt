btw we crash on all-caps config values such as SMTP for EZPDS_EMAIL_PROVIDER. file an issue for that please. I've also noticed a couple things in the identity wallet. during an account creation flow when first performing oauth for the app, we don't autofill the username, and additionally, the keyboard often uppercases the first letter causing the login to fail and is kind of annoying as a user. later in migration flows, we do prefill the username field, but we prefill the did and not the handle, which is a little strange as a user