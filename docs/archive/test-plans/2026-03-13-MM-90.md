# Test Plan: MM-90 DID Ceremony — Device-Signed Genesis Op

**Implementation plan:** `docs/implementation-plans/2026-03-13-MM-90/`
**Coverage:** 19/19 acceptance criteria — all automated

---

## Prerequisites

- Development shell active (`nix develop --impure --accept-flake-config` or direnv)
- `cargo test` passes with zero failures before this plan is run
- Bruno desktop app installed (for API collection inspection)

---

## Phase 1: Crypto Unit Tests

| Step | Action | Expected |
|------|--------|----------|
| 1.1 | `cargo test -p crypto -- verify_valid_op_returns_correct_fields` | Passes. `VerifiedGenesisOp` fields correct (did starts with `did:plc:`, 32 chars; `also_known_as` contains `at://alice.example.com`; `verification_methods` has `atproto`; `atproto_pds_endpoint` set). |
| 1.2 | `cargo test -p crypto -- verify_did_matches_build_did_plc_genesis_op` | Passes. DID from `verify_genesis_op` matches DID from `build_did_plc_genesis_op` (CBOR round-trip). |
| 1.3 | `cargo test -p crypto -- verify_wrong_rotation_key_returns_error` | Passes. Wrong key returns `CryptoError::PlcOperation`. |
| 1.4 | `cargo test -p crypto -- verify_corrupted_signature_returns_error` | Passes. Corrupted signature returns `CryptoError::PlcOperation`. |
| 1.5 | `cargo test -p crypto -- verify_unknown_fields_returns_error` | Passes. Unknown JSON field is rejected by `deny_unknown_fields`. |

---

## Phase 2: Integration Tests (Happy Path)

| Step | Action | Expected |
|------|--------|----------|
| 2.1 | `cargo test -p relay -- happy_path_promotes_account_and_returns_did` | Passes. Covers AC2.1–AC2.5 and AC4.1–AC4.3: HTTP 200, `did` starts with `did:plc:`, `status == "active"`, `did_document` is a JSON object, `alsoKnownAs` contains `at://{handle}`, `verificationMethod[0].publicKeyMultibase` starts with `z`, `service[0].serviceEndpoint == "https://test.example.com"`, all DB state transitions complete (accounts row, did_documents row, handles row, pending rows deleted). |
| 2.2 | `cargo test -p relay -- retry_with_pending_did_skips_plc_directory` | Passes. Retry with pre-stored `pending_did` returns 200 without calling plc.directory (wiremock `expect(0)` enforces this). |

---

## Phase 3: Integration Tests (Error Paths)

| Step | Action | Expected |
|------|--------|----------|
| 3.1 | `cargo test -p relay -- invalid_signature_returns_400` | Passes. Corrupted sig returns HTTP 400, `error.code == "INVALID_CLAIM"`. |
| 3.2 | `cargo test -p relay -- wrong_handle_in_op_returns_400` | Passes. Handle mismatch returns HTTP 400, `error.code == "INVALID_CLAIM"`. |
| 3.3 | `cargo test -p relay -- wrong_service_endpoint_returns_400` | Passes. Endpoint mismatch returns HTTP 400, `error.code == "INVALID_CLAIM"`. |
| 3.4 | `cargo test -p relay -- wrong_rotation_key_in_op_returns_400` | Passes. `rotationKeys[0]` mismatch returns HTTP 400, `error.code == "INVALID_CLAIM"`. (Crypto verification passes; semantic check fails.) |
| 3.5 | `cargo test -p relay -- already_promoted_account_returns_409` | Passes. Returns HTTP 409, `error.code == "DID_ALREADY_EXISTS"`. |
| 3.6 | `cargo test -p relay -- missing_auth_returns_401` | Passes. No `Authorization` header returns HTTP 401, `error.code == "UNAUTHORIZED"`. |
| 3.7 | `cargo test -p relay -- plc_directory_error_returns_502` | Passes. plc.directory returning 500 causes HTTP 502, `error.code == "PLC_DIRECTORY_ERROR"`. |

---

## Phase 4: Full Suite Validation

| Step | Action | Expected |
|------|--------|----------|
| 4.1 | `cargo test --workspace` | All 269 tests pass (45 common + 39 crypto + 185 relay). No regressions. |
| 4.2 | `cargo clippy --workspace -- -D warnings` | Zero warnings or errors. |
| 4.3 | `cargo fmt --all --check` | Formatting passes (exit code 0). |

---

## End-to-End: DID Ceremony Flow

**Purpose:** Validate the full provisioning pipeline (claim code → pending account → device → pending session → DID ceremony → promoted account).

1. Run `cargo test -p relay -- happy_path_promotes_account_and_returns_did` and observe the output. The test exercises the entire chain: inserts a claim code, creates a pending account, registers a device, creates a pending session, generates a P-256 keypair, builds and signs a genesis op, dispatches `POST /v1/dids`, and verifies all database state transitions complete atomically.
2. Confirm wiremock received exactly 1 POST request to plc.directory (enforced by `expect(1)`).
3. Run `cargo test -p relay -- retry_with_pending_did_skips_plc_directory` and confirm the retry path returns 200 without calling plc.directory (`expect(0)`).

---

## End-to-End: Semantic Validation Isolation

**Purpose:** Verify that AC3.2–AC3.4 semantic checks are independent from cryptographic verification. A valid signature must not bypass semantic validation.

1. `cargo test -p relay -- wrong_rotation_key_in_op_returns_400` — the op was signed by `kp_x` (crypto verification passes), but `rotationKeys[0]` is `kp_y`. Confirm HTTP 400.
2. `cargo test -p relay -- wrong_handle_in_op_returns_400` — valid signature, wrong `alsoKnownAs[0]`. Confirm HTTP 400.
3. `cargo test -p relay -- wrong_service_endpoint_returns_400` — valid signature, wrong `services.atproto_pds.endpoint`. Confirm HTTP 400.

---

## Bruno API Collection Inspection

| Step | Action | Expected |
|------|--------|----------|
| B.1 | Open `bruno/` collection in Bruno desktop app | Collection loads without errors. |
| B.2 | Inspect `create-did.bru` request body | Body contains `rotationKeyPublic` (string, `did:key:` URI) and `signedCreationOp` (JSON object). Method is POST, path is `/v1/dids`. No merge conflict markers. |
| B.3 | Verify authorization | `Authorization` header uses `Bearer {{pendingSessionToken}}`. |

---

## Traceability

| Acceptance Criterion | Automated Test | Manual Step |
|----------------------|----------------|-------------|
| AC1.1 | `verify_valid_op_returns_correct_fields` | 1.1 |
| AC1.2 | `verify_did_matches_build_did_plc_genesis_op` | 1.2 |
| AC1.3 | `verify_wrong_rotation_key_returns_error` | 1.3 |
| AC1.4 | `verify_corrupted_signature_returns_error` | 1.4 |
| AC1.5 | `verify_unknown_fields_returns_error` | 1.5 |
| AC2.1 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC2.2 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC2.3 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC2.4 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC2.5 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC2.6 | `retry_with_pending_did_skips_plc_directory` | 2.2 |
| AC3.1 | `invalid_signature_returns_400` | 3.1 |
| AC3.2 | `wrong_handle_in_op_returns_400` | 3.2 |
| AC3.3 | `wrong_service_endpoint_returns_400` | 3.3 |
| AC3.4 | `wrong_rotation_key_in_op_returns_400` | 3.4 |
| AC3.5 | `already_promoted_account_returns_409` | 3.5 |
| AC3.6 | `missing_auth_returns_401` | 3.6 |
| AC3.7 | `plc_directory_error_returns_502` | 3.7 |
| AC4.1 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC4.2 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| AC4.3 | `happy_path_promotes_account_and_returns_did` | 2.1 |
| Bruno collection (dev tooling) | N/A | B.1–B.3 |
