# Test Plan: MM-143 Tauri Mobile Project Scaffolding

Generated: 2026-03-14
Branch: `malpercio/mm-143-mobile-tauri-project-scaffolding-dev-environment`
Base SHA: `836d05ff05878920f7f073ffaf1fd9255d12601d`
Head SHA: `1d70c34f6251547474f8154305d1da3120f13d84`

---

## Automated Coverage

**16 / 16 acceptance criteria covered by automated checks.**

Run from the workspace root (inside the Nix dev shell):

```bash
# Rust workspace
cargo build                                    # AC2.2
cargo clippy --workspace -- -D warnings        # AC2.3
cargo fmt --all --check                        # AC2.4 (pre-existing relay failures on main — not introduced by this branch)
cargo test                                     # AC2.5

# Frontend
cd apps/identity-wallet
pnpm install --frozen-lockfile
pnpm build                                     # AC1.1 (files present), pnpm build succeeds
```

---

## Prerequisites

- macOS Ventura (13) or later
- Xcode installed (latest stable from App Store), launched at least once to accept the license agreement
- iOS Simulator platform installed: Xcode → Settings → Platforms → iOS
- Cocoapods installed: `sudo gem install cocoapods`
- Inside the Nix dev shell: `nix develop --impure --accept-flake-config` from the workspace root
- Dev shell tools verified:
  - `cargo-tauri --version` → `cargo-tauri-cli 2.x.x`
  - `node --version` → `v22.x.x`
  - `pnpm --version` → a version number

---

## One-Time Setup (run once per developer machine)

| Step | Action | Expected |
|------|--------|----------|
| 1 | `cd apps/identity-wallet && pnpm install` | Dependencies install cleanly; `node_modules/` created; no errors |
| 2 | `cargo tauri ios init` | Xcode project generated at `src-tauri/gen/apple/`; no errors |

---

## Phase 1: App Launch in iOS Simulator (AC3)

| Step | Action | Expected | AC |
|------|--------|----------|----|
| 1 | From `apps/identity-wallet/`, run `cargo tauri ios dev` | Vite dev server starts on port 5173, Rust compiles for `aarch64-apple-ios-sim`, iOS Simulator launches | — |
| 2 | Observe the Simulator after the build completes | App window appears in the iOS Simulator titled "Identity Wallet" | **AC3.1** |
| 3 | Observe the app content | Screen shows "Identity Wallet" heading, an input field pre-filled with "World", and a blue "Greet" button — NOT blank white, NOT an error page | **AC3.2** |
| 4 | Wait 10 seconds after the app loads | No crash dialog. App remains responsive (tapping input field works). | **AC3.3** |

---

## Phase 2: IPC Bridge Verification (AC4)

**Prerequisite:** App is running in the iOS Simulator from Phase 1.

| Step | Action | Expected | AC |
|------|--------|----------|----|
| 1 | Observe the input field | Contains "World" by default | — |
| 2 | Tap the **Greet** button | UI updates — a response message appears below the button. No freeze or error. | **AC4.1** |
| 3 | Read the response text | "Hello, World!" is displayed below the Greet button | **AC4.2** |
| 4 | Clear the input, type "Alice", tap **Greet** | "Hello, Alice!" replaces the previous greeting | **AC4.2** |
| 5 | Open Safari → Develop → [Simulator name] → WebView | Safari Web Inspector opens | — |
| 6 | Tap **Greet** once more in the Simulator | **Zero red error-level entries** appear in the Console. Warnings (yellow) are acceptable. | **AC4.3** |
| 7 | Select **Device → iPhone 15** (if not already) | All elements — heading, input field, Greet button, and greeting text — are visible simultaneously without scrolling | **AC4.4** |

---

## Phase 3: IPC Edge Cases

| Step | Action | Expected |
|------|--------|----------|
| 1 | Clear input completely, tap **Greet** | "Hello, !" is displayed. No crash or console error. |
| 2 | Enter 100+ character string, tap **Greet** | Full greeting displayed. No truncation. No crash. |
| 3 | Enter `<script>alert(1)</script>`, tap **Greet** | Response displays literally — no HTML injection, no alert dialog. |
| 4 | Rapidly tap **Greet** 10 times | Each tap produces a greeting. No crash, no freezing, no missing responses. |

---

## End-to-End: Full Developer Onboarding Flow

**Purpose:** Validates a new developer can go from a fresh clone to a running app, exercising AC1 through AC5 in sequence.

| Step | Action | Expected |
|------|--------|----------|
| 1 | Clone repo, enter directory | — |
| 2 | `nix develop --impure --accept-flake-config` | `cargo-tauri --version` shows 2.x; `node --version` shows v22.x; `pnpm --version` shows a version |
| 3 | `cargo build` from workspace root | Completes without errors — all 5 workspace members build including `identity-wallet` |
| 4 | `cd apps/identity-wallet && pnpm install` | `node_modules/` created, no errors |
| 5 | `cargo tauri ios init` | `src-tauri/gen/apple/` created |
| 6 | `cargo tauri ios dev` | iOS Simulator opens, Identity Wallet app launches with Greet demo |
| 7 | Tap **Greet** with default "World" input | "Hello, World!" appears |
| 8 | `git status` from workspace root | No files under `src-tauri/gen/` are tracked |

---

## End-to-End: Non-iOS Build Path (CI Equivalent)

**Purpose:** Validates the workspace builds cleanly without Xcode, simulating CI `rust-check` and `frontend-check` jobs.

| Step | Action | Expected |
|------|--------|----------|
| 1 | `cargo build` from workspace root | Succeeds |
| 2 | `cargo clippy --workspace -- -D warnings` | Succeeds, no warnings |
| 3 | `cargo test` | All tests pass; existing relay/crypto/common tests unaffected |
| 4 | `cd apps/identity-wallet && pnpm install && pnpm build` | SvelteKit static build succeeds; output in `apps/identity-wallet/dist/` |

---

## Traceability

| Acceptance Criterion | Automated | Manual Step |
|----------------------|-----------|-------------|
| AC1.1 — Frontend files exist | `test -f` on 7 files | — |
| AC1.2 — src-tauri files exist | `test -f` on 5 files | — |
| AC1.3 — SvelteKit 2.x / Svelte 5.x | `grep` on package.json | — |
| AC1.4 — Tauri 2.x | `grep` on src-tauri/Cargo.toml | — |
| AC2.1 — Workspace membership | `grep` on root Cargo.toml | — |
| AC2.2 — `cargo build` succeeds | `cargo build` | E2E Non-iOS, Step 1 |
| AC2.3 — `cargo clippy` passes | `cargo clippy --workspace -- -D warnings` | E2E Non-iOS, Step 2 |
| AC2.4 — `cargo fmt` passes | `cargo fmt --all --check` (pre-existing relay failures on main) | — |
| AC2.5 — Existing crates unaffected | `cargo test` | E2E Non-iOS, Step 3 |
| AC3.1 — App appears in simulator | — | Phase 1, Step 2 |
| AC3.2 — Placeholder screen visible | — | Phase 1, Step 3 |
| AC3.3 — No crash on launch | — | Phase 1, Step 4 |
| AC4.1 — Greet triggers Rust command | — | Phase 2, Step 2 |
| AC4.2 — Rust response displayed | — | Phase 2, Steps 3–4 |
| AC4.3 — No JS console errors | — | Phase 2, Steps 5–6 |
| AC4.4 — UI without scrolling (iPhone 15) | — | Phase 2, Step 7 |
| AC5.1 — cargo-tauri in PATH | `devenv.nix` declares `pkgs.cargo-tauri` | E2E Onboarding, Step 2 |
| AC5.2 — Node.js 22.x in PATH | `devenv.nix` declares `pkgs.nodejs_22` | E2E Onboarding, Step 2 |
| AC5.3 — pnpm in PATH | `devenv.nix` declares `pkgs.pnpm` | E2E Onboarding, Step 2 |
| AC5.4 — CLAUDE.md content | `grep` on `apps/identity-wallet/CLAUDE.md` | — |
| AC5.5 — Root CLAUDE.md pointer | `grep` on root `CLAUDE.md` | — |
| AC5.6 — src-tauri/gen/ gitignored | `grep` on `.gitignore` | E2E Onboarding, Step 8 |
| AC6.1 — rust-check documented | `grep` on design plan | — |
| AC6.2 — frontend-check documented | `grep` on design plan | — |
| AC6.3 — Simulator excluded from CI | `grep` on design plan | — |
