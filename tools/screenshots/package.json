{
  "name": "ezpds-docs-screenshots",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Playwright-driven docs screenshots: boots each mobile app's browser harness (VITE_HARNESS=fake) and captures deterministic per-scenario PNGs (happy + error/rare states) into sites/docs/. Linux-runnable, no macOS/Tauri shell.",
  "engines": {
    "node": ">=22.21"
  },
  "scripts": {
    "capture": "playwright test",
    "check": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
