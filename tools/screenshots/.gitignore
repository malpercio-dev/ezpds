# Playwright + tooling deps (regenerated by `pnpm install`).
node_modules/

# Visual-diff artifacts written by `capture.mjs --check` on drift (baselines live in sites/docs).
diff/

# Dev-server logs from the orchestrator (scripts/docs-screenshots.sh).
*-dev.log
